acl "allowed" {
        10.10.8.0/21;
};

options {
  directory "/var/cache/bind";

        recursion yes;
        allow-recursion { localhost; allowed; };
        listen-on port 53 { localhost; 10.10.13.1; };
        allow-query { localhost; allowed; };
        allow-transfer { none; };

        forwarders {
                10.10.12.254;
                8.8.8.8;
        };

        dnssec-validation auto;

        auth-nxdomain no;    # conform to RFC1035
        listen-on-v6 { none; };
};