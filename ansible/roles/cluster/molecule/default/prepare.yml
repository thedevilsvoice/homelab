---
- name: Prepare
  hosts: all
  gather_facts: false
  vars:
    molecule_no_log: false
  tasks:

    - name: "Create /etc/docker folder"
      file:
        path: "/etc/docker"
        state: directory
